to add required classes:
1. cp org folder in dist/
2. jar uf dss-calculator.jar org/
########
if some classes are still missed, add the content of the jar files in dist/lib/ to the org folder and repeat steps

